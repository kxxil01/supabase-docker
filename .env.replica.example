############
# Replica VM Environment Configuration
# Copy this to .env on VM2 (Replica)
############

############
# Database Configuration
# MUST MATCH MASTER VM SETTINGS
############

POSTGRES_PASSWORD=your-super-secret-and-long-postgres-password
POSTGRES_HOST=db
POSTGRES_DB=postgres
POSTGRES_PORT=5432

############
# Multi-VM Replication Setup
# UPDATE THESE IP ADDRESSES FOR YOUR ENVIRONMENT
############

# Master VM IP address (VM1)
POSTGRES_MASTER_HOST=192.168.1.10

# Replica VM IP address (VM2) - this VM
POSTGRES_REPLICA_HOST=192.168.1.11

# Replication user credentials (MUST MATCH MASTER)
POSTGRES_REPLICATION_USER=replicator
POSTGRES_REPLICATION_PASSWORD=replicator_pass

############
# Docker Configuration
############

# Docker socket location for monitoring
DOCKER_SOCKET_LOCATION=/var/run/docker.sock

############
# IMPORTANT NOTES FOR REPLICA VM:
############

# 1. This VM only runs PostgreSQL replica + monitoring
# 2. All passwords MUST match the master VM
# 3. Update IP addresses to match your network
# 4. Ensure network connectivity between VMs on port 5432
# 5. Deploy master VM first, then replica VM

############
# Deployment Command for VM2:
# docker compose -f docker-compose-replica.yml up -d
############
